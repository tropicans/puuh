---
phase: 06-core-feature-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06, VERIFY-07]
user_setup: []

must_haves:
  truths:
    - "All core components and E2E legislation upload and comparison flow are verified to work correctly"
  artifacts:
    - path: "docs/VERIFICATION-REPORT.md"
      provides: "Summary report of the audit results, showing status for each verified component"
      contains: "Core Feature Verification Report"
---

<objective>
Audit and verify the main features of the PUU Tracker application. This includes:
1. Docker Compose setup, Prisma migrations, and database seeding.
2. MinIO bucket creation, PDF upload, and storage URL generation.
3. PDF digital text extraction and scanned PDF Vision OCR chunking concurrency.
4. Article parsing and splitting, and verbatim LCS diff engine comparison logic.
5. Credentials authentication (NextAuth) and role-based guards.
6. A complete E2E workflow trace from upload to side-by-side comparison.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@GEMINI.md
@.planning/phases/06-core-feature-verification/06-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Verify Docker Environment, Build, and Database Setup</name>
  <files></files>
  <read_first>docker-compose.yml</read_first>
  <action>Verify that the Docker containers (postgres, minio, app) build and launch successfully. Run 'npm run lint' and 'npm run build' locally to check for build errors. Verify Prisma migration status and check that the seed-users script populates the database with default admin and viewer users using bcryptjs hashed passwords.</action>
  <verify>Check that docker compose ps shows running containers, npm run build succeeds, and seed-users runs without error.</verify>
  <acceptance_criteria>
    - Docker services start and remain running
    - 'npm run build' and 'npm run lint' pass with zero errors
    - Database is seeded successfully
  </acceptance_criteria>
  <done>Docker and DB environment verified</done>
</task>

<task type="auto">
  <name>Verify MinIO Bucket and Storage Integration</name>
  <files></files>
  <read_first>src/lib/minio.ts</read_first>
  <action>Audit MinIO configurations, check that the 'createbuckets' service successfully creates the 'puu-documents' bucket on startup, and verify that files uploaded through the API are stored correctly and accessible via server-to-server and browser contexts.</action>
  <verify>Check the MinIO bucket list and verify file uploads via MinIO client or curl.</verify>
  <acceptance_criteria>
    - 'puu-documents' bucket exists in MinIO
    - Uploading a mock PDF file succeeds and returns a valid proxy/storage URL
  </acceptance_criteria>
  <done>MinIO storage integration verified</done>
</task>

<task type="auto">
  <name>Verify PDF Text Extraction and Vision OCR Pipeline</name>
  <files></files>
  <read_first>src/lib/pdf-service.ts</read_first>
  <action>Verify the digital PDF parsing pipeline (pdfjs-dist / pdf-parse) extracts characters properly. Audit the ocr-service parallel chunk worker logic and Google Vision API key/proxy config. Ensure the single-page processing defensive fallback behaves correctly under failures.</action>
  <verify>Run 'npm run test:run' and check that ocr-service.test.ts passes. Inspect Vision API connection status.</verify>
  <acceptance_criteria>
    - ocr-service unit tests pass successfully
    - PDF text extraction falls back defensively when splitting or chunking fails
  </acceptance_criteria>
  <done>Text extraction and Vision OCR verified</done>
</task>

<task type="auto">
  <name>Verify Article Parsing and Verbatim LCS Diff Engine</name>
  <files></files>
  <read_first>src/lib/diff-engine.ts</read_first>
  <action>Verify the LLM-assisted article parser splits legal raw texts into JSON arrays of articles. Run all diff-engine unit tests to verify that word-level additions, deletions, replacements, case sensitivity, and boundary conditions are aligned with the LCS engine requirements.</action>
  <verify>Run 'npm run test:run' and check that diff-engine.test.ts passes.</verify>
  <acceptance_criteria>
    - diff-engine unit tests pass successfully
    - LCS diff engine correctly highlights differences between legislation articles
  </acceptance_criteria>
  <done>Article parsing and LCS diff engine verified</done>
</task>

<task type="auto">
  <name>Verify NextAuth Security Guards and E2E Workflow</name>
  <files></files>
  <read_first>src/middleware.ts</read_first>
  <action>Verify that route/page role guards redirect unauthenticated users to '/login' and restrict non-admin users from accessing '/upload' and '/manage'. Perform a complete E2E workflow on the application (login, upload two versions of a test legislation, parse them, view versions, and compare differences side-by-side) and compile the results into docs/VERIFICATION-REPORT.md.</action>
  <verify>Verify auth guards return 401/403 or redirect correctly. Generate docs/VERIFICATION-REPORT.md summarizing the results of all verification tasks.</verify>
  <acceptance_criteria>
    - Protected routes are secure and respect user roles
    - E2E flow is fully operational and verified
    - docs/VERIFICATION-REPORT.md is written and complete
  </acceptance_criteria>
  <done>Authentication security and E2E workflow verified</done>
</task>
</tasks>

<verification>
- [ ] Docker services, build, and seeding are verified
- [ ] MinIO storage bucket and upload flows are verified
- [ ] Text extraction and Vision OCR pipeline tests pass
- [ ] Article parsing and LCS diff engine tests pass
- [ ] Authentication guards and complete E2E comparison flow are verified
- [ ] docs/VERIFICATION-REPORT.md exists and contains the audit findings
</verification>

<success_criteria>
- All core application features are validated and working correctly
- docs/VERIFICATION-REPORT.md contains complete evidence and audit logs
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-feature-verification/SUMMARY.md`
</output>
