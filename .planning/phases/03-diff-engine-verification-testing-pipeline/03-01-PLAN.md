---
phase: 03-diff-engine-verification-testing-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vitest.config.ts, src/lib/diff-engine.test.ts]
autonomous: true
requirements: [TEST-01, TEST-02]
user_setup: []

must_haves:
  truths:
    - "Vitest runner is configured and integrated with tsconfig path aliases"
    - "Verbatim LCS diff engine is covered by automated unit tests"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config and path alias mapping"
      contains: "defineConfig"
    - path: "src/lib/diff-engine.test.ts"
      provides: "Unit tests verifying compareTexts and getDiffSummary"
      contains: "describe"
---

<objective>
Install and configure Vitest as the project test runner, map TypeScript path aliases to resolve imports correctly, and write a complete unit test suite for the verbatim LCS diff engine.

Purpose: Ensures regression safety and validates word-by-word diff calculation behavior across various text change scenarios.
Output: Configured package.json scripts, new vitest.config.ts configuration, and new src/lib/diff-engine.test.ts unit test file.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/lib/diff-engine.ts
@.planning/phases/03-diff-engine-verification-testing-pipeline/03-CONTEXT.md
@.planning/phases/03-diff-engine-verification-testing-pipeline/03-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Install Vitest dependencies and register package scripts</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Install vitest, @vitejs/plugin-react, and vite-tsconfig-paths as devDependencies using npm, then add test scripts ("test": "vitest", "test:run": "vitest run", "test:watch": "vitest watch") to package.json scripts.</action>
  <verify>npm run test:run exits with code 1 (no test files found yet, which is expected)</verify>
  <acceptance_criteria>
    - package.json contains vitest devDependency
    - package.json contains vite-tsconfig-paths devDependency
    - package.json contains test scripts: test, test:run, test:watch
  </acceptance_criteria>
  <done>Testing dependencies installed and package.json updated</done>
</task>

<task type="auto">
  <name>Create vitest.config.ts configuration file</name>
  <files>vitest.config.ts</files>
  <read_first>vitest.config.ts</read_first>
  <action>Create a new configuration file vitest.config.ts in the project root root directory using defineConfig from vitest/config. Configure it to load the react() and tsconfigPaths() plugins, set the test environment to "node", and include "src/**/*.test.ts" and "src/**/*.test.tsx" files.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - vitest.config.ts exists in root directory
    - vitest.config.ts imports tsconfigPaths and react plugins
    - test.environment is set to 'node'
  </acceptance_criteria>
  <done>Vitest runner configured with tsconfig mapping</done>
</task>

<task type="auto">
  <name>Write unit test suite for diff engine</name>
  <files>src/lib/diff-engine.test.ts</files>
  <read_first>src/lib/diff-engine.ts</read_first>
  <action>Create a new unit test file src/lib/diff-engine.test.ts adjacent to the diff engine source code. Write describe blocks for compareTexts and getDiffSummary, covering scenarios: identical strings, additions at end/middle, deletions at end, replacements (mixed changes), empty input boundaries, and case sensitivity.</action>
  <verify>npm run test:run exits with code 0 and passes all test scenarios</verify>
  <acceptance_criteria>
    - src/lib/diff-engine.test.ts contains test suite for compareTexts and getDiffSummary
    - Imports diff-engine module using path alias "@/lib/diff-engine"
    - All unit tests pass successfully
  </acceptance_criteria>
  <done>Automated unit tests written for LCS diff engine</done>
</task>
</tasks>

<verification>
- [ ] npm run test:run runs and passes all tests successfully
- [ ] tsc --noEmit passes successfully
- [ ] npm run build compiles without errors
</verification>

<success_criteria>
- Vitest testing framework is successfully installed and integrated
- Test commands run correctly using standard npm scripts
- Word-level additions and deletions are verified by unit tests with 100% pass rate
</success_criteria>

<output>
After completion, create `.planning/phases/03-diff-engine-verification-testing-pipeline/03-01-SUMMARY.md`
</output>
