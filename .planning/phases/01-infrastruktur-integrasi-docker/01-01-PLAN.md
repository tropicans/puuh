---
phase: 01-infrastruktur-integrasi-docker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["docker-compose.yml", ".env", ".env.example"]
autonomous: true
requirements: ["INF-01", "INF-02", "INF-03"]
user_setup: []
must_haves:
  truths:
    - "Service docling-serve is running on port 5001"
    - "Next.js app container environment contains DOCLING_API_URL pointing to http://docling-serve:5001"
    - "Health check endpoint for docling-serve is operational and reports healthy status"
  artifacts:
    - "docker-compose.yml"
    - ".env"
    - ".env.example"
  key_links: []
---

<objective>
Configure the docling-serve service in the docker-compose setup and integrate it with the Next.js app service.

Purpose: Provide layout-aware PDF extraction microservice infrastructure for the PUU Tracker.
Output: Configured docker-compose.yml, updated .env and .env.example files.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.yml
@.env
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure docling-serve and app environment in docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Modify docker-compose.yml to:
    1. Add the `docling-serve` service using image `quay.io/docling-project/docling-serve-cpu:v1.29.0`, with container_name `puu-tracker-docling`, restart policy `always`, ports `5001:5001`, environment `DOCLING_SERVE_ENABLE_UI=1`, and healthcheck:
       - test: ["CMD", "curl", "-f", "http://localhost:5001/health"]
       - interval: 10s
       - timeout: 5s
       - retries: 5
       - start_period: 20s
    2. Add `DOCLING_API_URL=${DOCLING_API_URL:-http://docling-serve:5001}` to the `app` service environment block.
    3. Add `docling-serve` dependency to the `app` service `depends_on` block with `condition: service_started`.
  </action>
  <verify>
    Verify docker-compose.yml structure is valid and contains the docling-serve definitions.
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains quay.io/docling-project/docling-serve-cpu:v1.29.0
    - docker-compose.yml contains DOCLING_API_URL pointing to http://docling-serve:5001
  </acceptance_criteria>
  <done>
    docker-compose.yml is updated with docling-serve service and app integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure environment variables in .env and .env.example</name>
  <files>.env, .env.example</files>
  <read_first>.env, .env.example</read_first>
  <action>
    Add DOCLING_API_URL="http://localhost:5001" to both .env and .env.example.
  </action>
  <verify>
    Check that .env and .env.example contain the DOCLING_API_URL definition.
  </verify>
  <acceptance_criteria>
    - .env contains DOCLING_API_URL="http://localhost:5001"
    - .env.example contains DOCLING_API_URL="http://localhost:5001"
  </acceptance_criteria>
  <done>
    Environment variables are added to .env and .env.example.
  </done>
</task>

</tasks>
