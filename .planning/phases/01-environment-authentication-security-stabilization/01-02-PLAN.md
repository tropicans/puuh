---
phase: 01-environment-authentication-security-stabilization
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/actions/users.ts, seed-users.ts, src/app/upload/layout.tsx, src/app/manage/layout.tsx]
autonomous: true
requirements: [SEC-03, SEC-04]
user_setup: []

must_haves:
  truths:
    - "Unauthenticated and non-admin users visiting /upload are redirected to /login"
    - "Unauthenticated and non-admin users visiting /manage are redirected to /login"
    - "Seeding script runs and retrieves admin and viewer passwords from environment variables"
  artifacts:
    - path: "src/app/upload/layout.tsx"
      provides: "Layout page security guard for upload route"
      contains: "redirect"
    - path: "src/app/manage/layout.tsx"
      provides: "Layout page security guard for manage route"
      contains: "redirect"
    - path: "src/actions/users.ts"
      provides: "User actions using environment variable seed credentials"
      contains: "SEED_ADMIN_PASSWORD"
---

<objective>
Implement server-side page-level route security layout guards to protect `/upload` and `/manage` pages from non-admin/unauthenticated users, and refactor the seeding logic to load passwords from environment variables.

Purpose: Secures administrative functions from unauthorized access, and protects seed passwords by avoiding hardcoded credentials in the repository.
Output: Route security layouts `src/app/upload/layout.tsx` and `src/app/manage/layout.tsx`, and updated `src/actions/users.ts` and `seed-users.ts`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/actions/users.ts
@seed-users.ts
@src/lib/authorization.ts
</context>

<tasks>
<task type="auto">
  <name>Create route layout guard for upload page</name>
  <files>src/app/upload/layout.tsx</files>
  <read_first>src/app/upload/page.tsx, src/lib/authorization.ts</read_first>
  <action>Create a new layout server component at src/app/upload/layout.tsx that imports getCurrentUser() and isAdminRole() from @/lib/authorization, and redirect from next/navigation. Guard the children by redirecting unauthenticated or non-admin users to /login.</action>
  <verify>test -f src/app/upload/layout.tsx</verify>
  <acceptance_criteria>
    - src/app/upload/layout.tsx contains getCurrentUser() and isAdminRole()
    - src/app/upload/layout.tsx redirects non-admin users to "/login"
  </acceptance_criteria>
  <done>upload route layout guard created</done>
</task>

<task type="auto">
  <name>Create route layout guard for manage page</name>
  <files>src/app/manage/layout.tsx</files>
  <read_first>src/app/manage/page.tsx, src/lib/authorization.ts</read_first>
  <action>Create a new layout server component at src/app/manage/layout.tsx that functions identically to the upload guard, ensuring that /manage and all its subpages are secured at the layout level.</action>
  <verify>test -f src/app/manage/layout.tsx</verify>
  <acceptance_criteria>
    - src/app/manage/layout.tsx contains getCurrentUser() and isAdminRole()
    - src/app/manage/layout.tsx redirects non-admin users to "/login"
  </acceptance_criteria>
  <done>manage route layout guard created</done>
</task>

<task type="auto">
  <name>Decouple seed credentials from codebase</name>
  <files>src/actions/users.ts, seed-users.ts</files>
  <read_first>src/actions/users.ts, seed-users.ts</read_first>
  <action>Modify src/actions/users.ts and seed-users.ts to fetch default passwords from process.env.SEED_ADMIN_PASSWORD and process.env.SEED_VIEWER_PASSWORD, falling back to 'admin123' and 'viewer123' if they are not set.</action>
  <verify>grep "SEED_ADMIN_PASSWORD" src/actions/users.ts</verify>
  <acceptance_criteria>
    - src/actions/users.ts contains SEED_ADMIN_PASSWORD and SEED_VIEWER_PASSWORD references
    - seed-users.ts contains SEED_ADMIN_PASSWORD and SEED_VIEWER_PASSWORD references
  </acceptance_criteria>
  <done>Seed credentials decoupled from hardcoded strings</done>
</task>
</tasks>

<verification>
- [ ] upload route layout guard redirects unauthorized users to /login
- [ ] manage route layout guard redirects unauthorized users to /login
- [ ] Seeding script reads credentials from environment variables
</verification>

<success_criteria>
- Unauthenticated or non-admin access to `/upload` or `/manage` is blocked on layout level
- Decoupled admin/viewer credentials loaded from environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-authentication-security-stabilization/01-02-SUMMARY.md`
</output>
