---
phase: 01-environment-authentication-security-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env, prisma/schema.prisma]
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup: []

must_haves:
  truths:
    - "Local Prisma commands can connect to the database on port 5434"
    - "Prisma migrate status shows up-to-date migrations"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Prisma schema database configuration"
      contains: "url = env(\"DATABASE_URL\")"
    - path: ".env"
      provides: "Environment variables configuration"
      contains: "5434"
---

<objective>
Align the database port mapping between local development and the Docker configuration, and generate the initial Prisma migration files to establish a version-controlled database schema workflow.

Purpose: Ensures that running the application and database tools locally succeeds without connection errors, and replaces direct db push operations with version-controlled migrations.
Output: Updated database URL port in .env, modified Prisma datasource URL in schema.prisma, and generated init_schema migrations folder.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.env
@prisma/schema.prisma
</context>

<tasks>
<task type="auto">
  <name>Align database port in environment configuration</name>
  <files>.env</files>
  <read_first>.env, docker-compose.yml</read_first>
  <action>Change the port in DATABASE_URL in .env from 5433 to 5434 to match the mapped host port in docker-compose.yml. Add SEED_ADMIN_PASSWORD="admin123" and SEED_VIEWER_PASSWORD="viewer123" placeholder variables to the env configuration file.</action>
  <verify>grep "5434" .env</verify>
  <acceptance_criteria>
    - .env contains "5434" inside DATABASE_URL
    - .env contains SEED_ADMIN_PASSWORD
    - .env contains SEED_VIEWER_PASSWORD
  </acceptance_criteria>
  <done>.env updated with port 5434 and seed placeholders</done>
</task>

<task type="auto">
  <name>Update Prisma schema datasource configuration</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>Modify the datasource db block in prisma/schema.prisma to include url = env("DATABASE_URL"). Ensure that other fields in the db block remain untouched.</action>
  <verify>grep "url = env(\"DATABASE_URL\")" prisma/schema.prisma</verify>
  <acceptance_criteria>
    - prisma/schema.prisma datasource db block contains url = env("DATABASE_URL")
  </acceptance_criteria>
  <done>schema.prisma updated to reference the env database URL</done>
</task>

<task type="auto">
  <name>Generate and apply initial database migration</name>
  <files>prisma/migrations/</files>
  <read_first>prisma/schema.prisma, docker-compose.yml</read_first>
  <action>Start the Postgres database container if not running, then execute npx prisma migrate dev --name init_schema. This will generate the SQL migration file and apply it to the database, creating the migrations directory.</action>
  <verify>npx prisma migrate status</verify>
  <acceptance_criteria>
    - A subdirectory is created inside prisma/migrations/ containing migration.sql
    - npx prisma migrate status exits with code 0 and reports no pending migrations
  </acceptance_criteria>
  <done>Initial migration created and applied successfully</done>
</task>
</tasks>

<verification>
- [ ] DATABASE_URL port is 5434 in .env
- [ ] prisma/schema.prisma datasource db contains url = env("DATABASE_URL")
- [ ] prisma/migrations/ directory exists with the init_schema migration files
- [ ] npx prisma migrate status runs successfully without errors
</verification>

<success_criteria>
- Local Prisma CLI commands successfully connect to the PostgreSQL instance
- Initial database schema migration is successfully created and tracked in git
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-authentication-security-stabilization/01-01-SUMMARY.md`
</output>
