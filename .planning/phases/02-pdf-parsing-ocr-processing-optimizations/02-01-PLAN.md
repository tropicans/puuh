---
phase: 02-pdf-parsing-ocr-processing-optimizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/ocr-service.ts]
autonomous: true
requirements: [PERF-01]
user_setup: []

must_haves:
  truths:
    - "OCR chunk processing runs concurrently with up to 3 active requests"
    - "OCR concurrency limit is configurable via OCR_CONCURRENCY_LIMIT env variable"
  artifacts:
    - path: "src/lib/ocr-service.ts"
      provides: "Vision OCR concurrency and splitting logic"
      contains: "runWithConcurrencyLimit"
---

<objective>
Implement a concurrency-limiting helper in the OCR service and parallelize the extraction of PDF text chunks to optimize processed document upload speeds.

Purpose: Speeds up text extraction for scanned documents without triggering rate limits on the Gemini API.
Output: Refactored ocr-service.ts with Promise pooling chunk processing.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ocr-service.ts
</context>

<tasks>
<task type="auto">
  <name>Implement concurrency limiter helper function</name>
  <files>src/lib/ocr-service.ts</files>
  <read_first>src/lib/ocr-service.ts</read_first>
  <action>Add the runWithConcurrencyLimit helper function at the bottom of src/lib/ocr-service.ts to execute a list of promise factories with a maximum concurrency limit.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - src/lib/ocr-service.ts contains runWithConcurrencyLimit function definition
  </acceptance_criteria>
  <done>runWithConcurrencyLimit helper implemented</done>
</task>

<task type="auto">
  <name>Refactor extractTextWithVision to use concurrent pooling</name>
  <files>src/lib/ocr-service.ts</files>
  <read_first>src/lib/ocr-service.ts</read_first>
  <action>Refactor the extractTextWithVision function in src/lib/ocr-service.ts to build all PDF chunk buffers upfront, map them to chunk vision extraction tasks, read OCR_CONCURRENCY_LIMIT from environment (defaulting to 3), and execute them concurrently via runWithConcurrencyLimit.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - extractTextWithVision uses runWithConcurrencyLimit
    - extractTextWithVision reads OCR_CONCURRENCY_LIMIT environment variable
  </acceptance_criteria>
  <done>extractTextWithVision parallelized with limit control</done>
</task>
</tasks>

<verification>
- [ ] tsc --noEmit passes successfully
- [ ] npm run build compiles without errors
</verification>

<success_criteria>
- Vision OCR chunk requests execute concurrently up to the configured limit
- Larger PDF files split and process text extraction concurrently
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-parsing-ocr-processing-optimizations/02-01-SUMMARY.md`
</output>
