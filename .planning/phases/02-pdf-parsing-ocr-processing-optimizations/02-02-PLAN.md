---
phase: 02-pdf-parsing-ocr-processing-optimizations
plan: 02
type: execute
wave: 1
depends_on: ["02-01-PLAN"]
files_modified: [src/lib/ai-service.ts, src/lib/storage.ts, src/app/api/documents/[...path]/route.ts]
autonomous: true
requirements: [PERF-02, PERF-03]
user_setup: []

must_haves:
  truths:
    - "Regex-based article splitter matches OCR typos of 'Pasal' (e.g. 'Pasa1', 'Pas al') and alphanumeric numbers (e.g. 'Pasal 103A')"
    - "MinIO document URLs resolve correctly for browser clients by proxying requests through Next.js API route"
  artifacts:
    - path: "src/lib/ai-service.ts"
      provides: "Regex-based article splitter hardening"
      contains: "parseArticlesWithRegex"
    - path: "src/lib/storage.ts"
      provides: "Storage configuration returning relative proxy paths"
      contains: "uploadFile"
    - path: "src/app/api/documents/[...path]/route.ts"
      provides: "Next.js API route serving files from MinIO"
      contains: "GET"
---

<objective>
Harden the regex-based article splitter to handle OCR spelling mistakes and alphanumeric numbers, and create a Next.js API route proxy to resolve MinIO download hostname differences.

Purpose: Ensures legal articles are split correctly regardless of minor OCR typos, and PDF downloads succeed for both Docker container contexts and external browser clients.
Output: Refactored ai-service.ts, refactored storage.ts, and a new dynamic API route under api/documents/[...path]/route.ts.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/ai-service.ts
@src/lib/storage.ts
</context>

<tasks>
<task type="auto">
  <name>Harden parseArticlesWithRegex in ai-service.ts</name>
  <files>src/lib/ai-service.ts</files>
  <read_first>src/lib/ai-service.ts</read_first>
  <action>Refactor parseArticlesWithRegex to split the text using a lookahead pattern matching "Pasal" OCR variations (e.g. Pasa[l1], Pas\s*al) and alphanumeric suffixes (e.g., 103A), extract the matches, and normalize the article numbers to the standard "Pasal [Number]" format.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - parseArticlesWithRegex supports spelling anomalies like Pasa1 and Pas al
    - parseArticlesWithRegex supports alphanumeric numbers like 103A
    - parseArticlesWithRegex normalizes matched titles before return
  </acceptance_criteria>
  <done>Article parser regex hardened and normalized</done>
</task>

<task type="auto">
  <name>Refactor uploadFile in storage.ts to return relative proxy URL</name>
  <files>src/lib/storage.ts</files>
  <read_first>src/lib/storage.ts</read_first>
  <action>Update uploadFile in src/lib/storage.ts to return a relative URL path in the format `/api/documents/${filename}` instead of the absolute MinIO localhost hostname and port.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - storage.uploadFile returns relative path prefixing `/api/documents/`
  </acceptance_criteria>
  <done>File storage configured to return relative proxy routes</done>
</task>

<task type="auto">
  <name>Create Next.js API route proxy for MinIO documents</name>
  <files>src/app/api/documents/[...path]/route.ts</files>
  <read_first>src/lib/storage.ts</read_first>
  <action>Create a new route file src/app/api/documents/[...path]/route.ts implementing a GET handler. It must extract the file path from params, call storage.getFileStream(filename) to fetch the file stream from MinIO, convert the Node.js Readable stream to a Web ReadableStream using Readable.toWeb(), and return a Response with proper Content-Type ('application/pdf') and inline Content-Disposition headers.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - New API route exists at src/app/api/documents/[...path]/route.ts
    - API route GET handler calls storage.getFileStream
    - API route GET handler serves PDF inline with correct content headers
  </acceptance_criteria>
  <done>Dynamic document proxy route created</done>
</task>
</tasks>

<verification>
- [ ] tsc --noEmit passes successfully
- [ ] npm run build compiles without errors
</verification>

<success_criteria>
- Article parser parses pages with "Pasa1" or "Pas al" typos and alphanumeric numbers correctly
- Documents uploaded to MinIO are downloadable by both docker and browser clients using dynamic proxy routing without hardcoded port mappings
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-parsing-ocr-processing-optimizations/02-02-SUMMARY.md`
</output>
