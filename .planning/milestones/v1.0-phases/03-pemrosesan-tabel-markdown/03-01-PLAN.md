---
phase: 03-pemrosesan-tabel-markdown
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/utils.ts", "src/lib/pdf-service.ts", "src/app/api/upload/route.ts", "src/app/api/regulations/fetch/route.ts", "src/app/api/versions/[id]/reupload/route.ts", "src/__tests__/utils.test.ts", "src/lib/pdf-service.test.ts"]
autonomous: true
requirements: ["TAB-01", "TAB-02"]
user_setup: []
must_haves:
  truths:
    - "D-01: cleanMarkdownText is implemented separately from cleanPdfText and does not strip solitary digits"
    - "D-02: cleanPdfText is left unmodified and is used on fallback paths"
    - "D-03: Substring truncation is removed from upload, fetch, and reupload routes to store rawText fully"
    - "D-04: Text content is stored directly in database; no secondary storage in MinIO"
    - "D-05: Fallback warning message is prepended to rawText when extractionMethod is not docling"
    - "D-06: Markdown table content from Docling is saved as-is without manual normalizations"
  artifacts:
    - "src/lib/utils.ts"
    - "src/lib/pdf-service.ts"
    - "src/app/api/upload/route.ts"
    - "src/app/api/regulations/fetch/route.ts"
    - "src/app/api/versions/[id]/reupload/route.ts"
  key_links: []
---

<objective>
Implement a table-safe text cleaning utility for Docling output, disable database rawText string length limits during uploads, and prepend a clear warning header to the database record whenever a non-Docling fallback occurs.
</objective>

<threat_model>
## Security Threat Model

- **T-03-01: Database injection or buffer issues with extremely large text insertions**
  - **Severity:** Medium
  - **Mitigation:** Ensure database transactions are handled atomically via Prisma client, and limit incoming PDF request size to 20MB in Next.js router. Validate that rawText can be of arbitrary length up to PostgreSQL's limit (1GB).
</threat_model>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pemrosesan-tabel-markdown/03-CONTEXT.md
@src/lib/utils.ts
@src/lib/pdf-service.ts
@src/app/api/upload/route.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add test coverage for cleanMarkdownText and solitary numbers preservation</name>
  <files>src/__tests__/utils.test.ts, src/lib/pdf-service.test.ts</files>
  <read_first>src/__tests__/utils.test.ts</read_first>
  <action>
    Add test cases to:
    1. src/__tests__/utils.test.ts validating `cleanMarkdownText` correctly trims headers, footers, and page numbers while retaining solitary digits.
    2. src/lib/pdf-service.test.ts validating that when Docling is used, solitary digits and table formats are preserved in the returned text.
  </action>
  <verify>
    Run vitest suites:
    `npx vitest run src/__tests__/utils.test.ts`
    `npx vitest run src/lib/pdf-service.test.ts`
  </verify>
  <acceptance_criteria>
    - Tests for cleanMarkdownText and solitary digits exist and pass successfully
  </acceptance_criteria>
  <done>
    Unit tests for markdown cleaning and preservation are established and green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement cleanMarkdownText and integrate it with Docling client flow</name>
  <files>src/lib/utils.ts, src/lib/pdf-service.ts</files>
  <read_first>src/lib/utils.ts</read_first>
  <action>
    1. Implement cleanMarkdownText in src/lib/utils.ts.
    2. Update smartExtractPdfText in src/lib/pdf-service.ts to use cleanMarkdownText instead of cleanPdfText for Docling extraction output.
  </action>
  <verify>
    Run vitest to ensure all tests pass:
    `npx vitest run src/__tests__/utils.test.ts`
    `npx vitest run src/lib/pdf-service.test.ts`
  </verify>
  <acceptance_criteria>
    - cleanMarkdownText handles headers, footers, page numbers and leaves single digits intact
    - Docling path in smartExtractPdfText calls cleanMarkdownText
  </acceptance_criteria>
  <done>
    Docling outputs are cleaned using cleanMarkdownText and solitary numbers are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove string limits and prepend warning header on fallback</name>
  <files>src/app/api/upload/route.ts, src/app/api/regulations/fetch/route.ts, src/app/api/versions/[id]/reupload/route.ts</files>
  <read_first>src/app/api/upload/route.ts</read_first>
  <action>
    1. In src/app/api/upload/route.ts:
       - If `extractionMethod !== 'docling'`, prepend warning: `[PERINGATAN: Dokumen ini diproses menggunakan metode fallback (${extractionMethod}). Struktur tabel mungkin tidak terurai dengan sempurna.]\n\n` to rawText.
       - Remove `.substring(0, 100000)` limit when storing rawText.
    2. In src/app/api/regulations/fetch/route.ts:
       - Remove `.substring(0, 100000)` limit when storing rawText.
    3. In src/app/api/versions/[id]/reupload/route.ts:
       - Remove `.substring(0, 100000)` limit when storing rawText.
  </action>
  <verify>
    Verify compilation and lint:
    `npm run lint`
  </verify>
  <acceptance_criteria>
    - All routes save the full rawText without substring limits
    - Fallback warning is prepended dynamically when extraction method is not docling
    - Project compiles and lints cleanly
  </acceptance_criteria>
  <done>
    Persistence constraints and fallback indicators are implemented.
  </done>
</task>

</tasks>

## Artifacts this phase produces

- **Symbols:** `cleanMarkdownText` in `src/lib/utils.ts`
- **Side Effects:** rawText saved to database has no 100K char limit, and fallback paths have a prepended warning label.
