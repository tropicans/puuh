---
phase: 04-llm-ui-integration
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified: ["prisma/schema.prisma", "src/lib/ai-service.ts", "src/lib/regulation-fetcher.ts", "src/app/api/regulations/fetch/route.ts", "src/app/api/upload/route.ts", "src/app/regulations/[id]/page.tsx", "src/components/regulations/VersionTimeline.tsx"]
autonomous: true
requirements: ["AI-01", "UI-01"]
user_setup: []
must_haves:
  truths:
    - "D-01: LLM prompt preserves table Markdown formatting without summarization."
    - "D-02: LLM prompt preserves hierarchical list Markdown formatting and indentation."
    - "D-03: Heuristic validation comparing occurrences of Pasal in text with parsed articles count triggers regex fallback on discrepancy."
    - "D-04: System prompt specifies rules for amendment processing (extracting only new/changed articles)."
    - "D-05: Regulation fetcher calls smartExtractPdfText as first strategy."
    - "D-07: Fallback warning is prepended to rawText on auto-search fallback."
    - "D-09: extractionMethod column added in prisma schema."
    - "D-10: Minimalist badges displaying extractionMethod in VersionTimeline."
  artifacts:
    - "prisma/schema.prisma"
    - "src/lib/ai-service.ts"
    - "src/lib/regulation-fetcher.ts"
    - "src/app/api/regulations/fetch/route.ts"
    - "src/app/api/upload/route.ts"
    - "src/app/regulations/[id]/page.tsx"
    - "src/components/regulations/VersionTimeline.tsx"
  key_links: []
---

<objective>
Refactor LLM prompts to accurately process structured table and list Markdown formatting, implement heuristic validation on parsed article counts, refactor auto-search to use smart PDF extraction, record extractionMethod in database, and display color-coded extraction badges on the version timeline.
</objective>

<tasks>
<task type="tdd">
  <name>Task 1: Add unit tests for parsed article count heuristics and fallbacks</name>
  <files>src/lib/ai-service.test.ts</files>
  <action>
    Create src/lib/ai-service.test.ts containing tests verifying normal article parsing, heuristic validation triggering fallback on discrepancy, and malformed JSON fallback.
  </action>
  <verify>
    `npx vitest run src/lib/ai-service.test.ts`
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement prompt improvements and heuristics in ai-service.ts</name>
  <files>src/lib/ai-service.ts</files>
  <action>
    Update parseArticlesFromText to enforce list and table retention rules, instruct only changed articles on amendments, and validate output length using the Pasal match count heuristic.
  </action>
  <verify>
    `npx vitest run src/lib/ai-service.test.ts`
  </verify>
</task>

<task type="auto">
  <name>Task 3: Refactor database schema and auto-search logic</name>
  <files>prisma/schema.prisma, src/lib/regulation-fetcher.ts, src/app/api/regulations/fetch/route.ts, src/app/api/upload/route.ts</files>
  <action>
    1. Add extractionMethod String? to RegulationVersion model.
    2. Refactor regulation-fetcher.ts to use smartExtractPdfText.
    3. Save extractionMethod to DB in upload and fetch routes.
  </action>
  <verify>
    `npx prisma db push && npx prisma generate`
  </verify>
</task>

<task type="auto">
  <name>Task 4: Add UI badges to version timeline</name>
  <files>src/app/regulations/[id]/page.tsx, src/components/regulations/VersionTimeline.tsx</files>
  <action>
    Expose and map extractionMethod in RegulationDetailPage and display styled extraction badges on the timeline in VersionTimeline.tsx.
  </action>
  <verify>
    Verify compilation and run development server check.
  </verify>
</task>
</tasks>
